# Open questions on the redesigned pages

## /privacy

1. The third-party services list describes **Cloudflare** as providing "DNS, CDN, and protection for the public website." Is this still accurate to the actual current DNS/CDN setup?

## Layout consistency

2. `/privacy` and `/terms` both have a sticky sidebar table-of-contents (anchor links to each section). `/security` is a similarly long page but doesn't have one. Want the same sticky-TOC treatment added to `/security` for consistency?

## Long-tail SEO pages

3. So far dropped: `/share-password-securely`, `/send-api-key-securely`, `/one-time-secret-link` — each added to `sitemap.ts` as it landed. Confirming the remaining pages from the original 10-page plan are still coming: `/share-env-file-securely`, `/slack-password-sharing-alternative`, `/email-password-sharing-alternative`, `/send-login-credentials-securely`, `/secure-password-sharing-for-contractors`, `/secure-secret-sharing-for-freelancers`, `/temporary-secret-link`.
